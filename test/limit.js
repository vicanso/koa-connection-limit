"use strict";
const assert = require('assert');
const koa = require('koa');
const koaConnectionLimit = require('../lib/limit');

describe('koa-connection-limit', function(){
  it('should set connection limit successful', function(){
  });
});
